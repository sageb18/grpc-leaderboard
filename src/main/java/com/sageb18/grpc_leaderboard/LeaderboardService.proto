syntax = "proto3";

service LeaderboardService {

  rpc SubmitScore (SubmitScoreRequest) returns (SubmitScoreResponse);

  rpc GetTopScores (GetTopTenRequest) returns (GetTopTenResponse);
}

message LeaderboardEntry {
  string user_id = 1;
  string username = 2;
  string score = 3;
}

message SubmitScoreRequest {

}